// Create a new HTML element to hold the message
var messageElement = document.createElement("p");

// Set the HTML content of the element to a message
messageElement.innerHTML = "Yah! I can write <strong>HTML</strong> content through JS";

// Append the element to the body of the document
document.body.appendChild(messageElement);
